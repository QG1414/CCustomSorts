#ifndef SORT_H // check if SORT_H was defined if not then elements below will be active but only once because it will be defined right after
#define SORT_H

// sort functions declarations
void simpleInsertion( int* pTab, int nSize );
void simpleSelection( int* pTab, int nSize );
void bubbleSort( int* pTab, int nSize );

#endif
